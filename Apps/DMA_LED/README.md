#DMA驱动查理复用LED点阵

256个LED连接在单片机PB0到PB15上，由CH32V203的DMA驱动显示。
驱动流程：

1. 所有引脚输出寄存器为0
2. DMA两个通道分别在同一时间写GPIO模式寄存器和输出寄存器，PB0变为输出并拉高，需要点亮的LED对应的引脚（如PB10和PB8）也变为输出，由于那些引脚输出寄存器本就为0,此时对应的引脚输出低电平，LED点亮。
3. 经过一段时间后，另一个DMA通道写所有引脚输出寄存器为0,LED灭，该行显示完成
4. 切换到下一行（PB1）并重复，以此类推。